{
  "name": "wissensmatrix",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "probe": "esbuild proben/baum.probe.ts --bundle --platform=node --format=esm --outfile=node_modules/.probe.mjs --log-level=error && node node_modules/.probe.mjs",
    "pruefe": "npm run probe && npm run build"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}